# Notepad Mono

Write and sync your monospace notes everywhere.

## Run

```shell
cd app
flutter run -d windows #--release for release mode
```

```shell
cd app
flutter run -d windows #--release for release mode
```
